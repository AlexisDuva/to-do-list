spring.application.name=to-do-list

spring.datasource.url=jdbc:postgresql://${PGHOST:localhost}:${PGPORT:5432}/${PGDATABASE:todolist}
spring.datasource.username=${PGUSER:todolist}
spring.datasource.password=${PGPASSWORD:todolist}

spring.jpa.hibernate.ddl-auto=validate
